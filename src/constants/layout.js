// Shared spacing tokens so every section uses the same rhythm.
// Values are MUI spacing units (1 = 8px) unless noted.

// Max content width for centered sections (Container "lg" ~ 1200px).
export const CONTENT_MAX_WIDTH = "lg";

// Vertical padding between the top/bottom of a section and its content.
export const SECTION_PY = { xs: 4, md: 6 };

// Horizontal page gutters for full-bleed sections (hero/banner).
export const SECTION_PX = { xs: 2, sm: 4, md: 8 };

// Max width for centered intro/body paragraphs (px).
export const TEXT_MAX_WIDTH = 720;
